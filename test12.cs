// The bot doesn't use `_currentBrake`. It only slows down by reducing throttle.
// So we can simply set `_currentBrake = 0` when computer controlled.
// Let's create an execution plan and review it.
